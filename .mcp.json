{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@0.0.75",
        "--isolated",
        "--allowed-origins",
        "http://localhost:*;http://127.0.0.1:*"
      ],
      "env": {}
    }
  }
}